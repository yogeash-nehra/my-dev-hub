# AI Developer Digest — 2026-07-11

*3 items passed quality gate | 42 scanned | 39 excluded | Sources checked: 24*

---

## This Week's Signal

> July 11 is the first quiet day after the dense July 9–10 cluster (Muse Spark 1.1, GPT-5.6, Grok 4.5). The headline developer change is Claude Code v2.1.207 flipping auto mode ON by default for Bedrock, Vertex AI, and Foundry users — no environment variable opt-in required anymore — while simultaneously hardening plugin security by rejecting `${user_config.*}` variable expansion in shell-form hook commands. The SWE-bench Pro leaderboard filled in its GPT-5.6 numbers for the first time: Sol (64.6%), Terra (63.4%), and Luna (62.7%) all land above Muse Spark 1.1 (61.5%) on pure coding, giving teams their first concrete apples-to-apples comparison across the full frontier API tier stack. llama.cpp continues its rapid cadence with 6 new builds in 22 hours, headlined by DeepSeek-OCR v1 multi-tile support (b9963) — the first native multi-document OCR capability in the GGUF toolchain.

**Must-reads this digest:**
- **Claude Code v2.1.207** — auto mode on by default on Bedrock/Vertex/Foundry, Opus 4.8 becomes the default model; review plugin hook configs before pulling the update
- **SWE-bench Pro: GPT-5.6 scores confirmed** — all three tiers (Sol 64.6%, Terra 63.4%, Luna 62.7%) beat Muse Spark 1.1 (61.5%) on coding; full tier comparison now available across Anthropic, OpenAI, and Meta

---

## [BREAKING] Breaking Changes

### [BREAKING] Claude Code v2.1.207 — plugin hook `${user_config.*}` rejected in shell-form commands; plugin settings removed from project-level config

**Source:** Anthropic / Claude Code Changelog | **Date:** July 11, 2026 | **Link:** https://code.claude.com/docs/en/changelog

**What changed:** Three security hardening changes in v2.1.207 affect developers who use Claude Code with custom plugin hooks or project-level settings:
1. `${user_config.*}` variable expansion is now rejected in shell-form plugin hook commands — previously allowed, now blocked (shell injection fix).
2. Plugin option values are no longer read from project-level `.claude/settings.json` — they must be configured at the user level.
3. Auto mode configuration is no longer read from `.claude/settings.local.json` — use `~/.claude/settings.json` (user-level settings) instead.

**TL;DR:** Three Claude Code v2.1.207 plugin configuration paths now silently fail or throw: `${user_config.*}` in shell-form hooks is blocked, plugin options from project-level config are ignored, and auto mode settings from `.claude/settings.local.json` are ignored.

**Developer signal:** Check three things before pulling v2.1.207: (1) Search your `.claude/` directories for any plugin hook definitions that reference `${user_config.*}` in shell-form command strings — replace with direct values or environment variables; (2) If you configured plugin options in a project-level `.claude/settings.json`, move those values to your user-level `~/.claude/settings.json`; (3) If you previously controlled auto mode from `.claude/settings.local.json` (project-scoped), move the setting to `~/.claude/settings.json`. These are security hardening changes: the `${user_config.*}` block prevents a class of shell injection via malicious repository `.claude/` files; the plugin project-settings block prevents plugin escalation through project-local config. If you run Claude Code in automated CI or agentic pipelines with custom plugins, audit your hook definitions before the update lands.

**Affects you if:** You use Claude Code with custom plugin hooks that use `${user_config.*}` variable expansion in shell-form commands; you configure plugin options or auto mode from project-level `.claude/settings.json` or `.claude/settings.local.json`.

**Adoption effort:** Moderate — configuration migration required; breaking only if you used the specific patterns above.

**Primary source:** https://code.claude.com/docs/en/changelog

*Quality gate score: 9 (official Anthropic source +3, code-level technical changes +2, primary source link +2, in-window +1, technical audience +1)*

---

## Model Releases

No model releases this period.

---

## API & SDK Changes

No new API or SDK breaking changes outside the Breaking section above. The Anthropic Platform release notes' most recent entry is a July 10 documentation update to the Access Transparency / CMEK content preservation page (added a filter example, an example event payload, and two preservation reason codes: `policy_violation_investigation` and `csae_report`). This is a documentation expansion, not a functional API change. The OpenAI, Google, Meta, and Mistral platform changelogs showed no new entries for July 10–11 beyond changes already covered in the July 9–10 digests.

---

## Research

Nothing cleared the quality bar this period. arXiv cs.AI and cs.CL new submissions for July 10–11 did not combine recognized-lab authorship, an associated code repository, and measurable benchmark numbers at the required threshold within the scan window. See Near-misses for details.

---

## Tooling

### [MEDIUM] Claude Code v2.1.207 — auto mode default on Bedrock, Vertex AI, and Foundry; Opus 4.8 becomes the new default model

**Source:** Anthropic / Claude Code Changelog | **Date:** July 11, 2026 | **Link:** https://code.claude.com/docs/en/changelog

**What changed:** Auto mode, previously requiring explicit `CLAUDE_CODE_ENABLE_AUTO_MODE=1` environment variable opt-in on AWS Bedrock, Google Vertex AI, and Microsoft Azure Foundry, is now on by default in all three environments. Simultaneously, the default model for all three cloud provider environments changed from Claude Opus 4.7 to Claude Opus 4.8.

**TL;DR:** Claude Code v2.1.207 enables auto mode by default on Bedrock/Vertex/Foundry and upgrades the default model to Opus 4.8 — developers must explicitly set `disableAutoMode: true` in `~/.claude/settings.json` to opt out.

**Developer signal:** Two distinct things changed in v2.1.207 that warrant immediate attention. (1) **Auto mode default flip**: Auto mode lets Claude Code autonomously decide which Claude model to use for each task within a session — switching between faster, cheaper models for simple steps and more capable models for complex reasoning steps. If your team has Claude Code deployed on Bedrock, Vertex, or Foundry without `CLAUDE_CODE_ENABLE_AUTO_MODE=1`, you were previously running in manual mode (single-model throughout). After v2.1.207, you're in auto mode. This changes cost patterns and response characteristics. To opt out: set `disableAutoMode: true` in `~/.claude/settings.json`. (2) **Opus 4.8 default**: The baseline model for Bedrock/Vertex/Foundry changes from Opus 4.7 to Opus 4.8. Opus 4.8 has improved agentic performance and was benchmarked at SWE-bench Pro 69.2%. If you have cost or behavior expectations anchored to Opus 4.7, test against Opus 4.8 before broadly deploying. (3) **Two critical bug fixes worth highlighting**: The terminal freeze fix (v2.1.207 resolves keystrokes lagging while streaming very long lists, tables, or code blocks — a serious UX issue for CI pipelines that log large outputs) and the managed-settings consent bug fix (non-interactive runs via `claude -p` or the SDK were permanently recording remote managed settings as consented without showing the security dialog — now fixed).

**Affects you if:** You run Claude Code on AWS Bedrock, Google Vertex AI, or Azure Foundry; you have cost or model-behavior expectations set against Opus 4.7 as the default; you use the Claude Code SDK in non-interactive pipelines with managed settings.

**Adoption effort:** Moderate — review auto mode implications and model change before deploying to automated workflows; plugin security changes require configuration audit (see Breaking Changes).

**Primary source:** https://code.claude.com/docs/en/changelog

*Quality gate score: 9 (official Anthropic source +3, code-level technical changes with specific flags and config paths +2, primary source link +2, in-window +1, technical audience +1)*

---

### [NOTABLE] llama.cpp b9957–b9966 — DeepSeek-OCR v1 multi-tile support, Hexagon ARGSORT HVX, tensor-split decode optimization

**Source:** ggml-org/llama.cpp GitHub Releases | **Date:** July 10–11, 2026 | **Link:** https://github.com/ggml-org/llama.cpp/releases

**What changed:** 6 new builds in 22 hours (b9957 through b9966, picking up from b9956 covered in the July 10 digest). Three changes stand out against the incremental background: DeepSeek-OCR v1 multi-tile support with dynamic resolution (b9963); Hexagon ARGSORT performance improvement via bitonic sort in HVX registers for up to 1024 elements (b9965); and tensor-split regex patterns made static to eliminate per-call recompilation in `-sm tensor` multi-GPU decode mode (b9966). Additionally, b9957 removes `apply_diff` from the server tools layer and introduces a `tools_io` abstraction.

| Build | UTC | Key change |
|-------|-----|------------|
| b9966 | Jul 11, 18:54 | Tensor-split regex patterns made static — prevents regex recompilation on each decode call in `-sm tensor` mode |
| b9965 | Jul 11, 18:11 | Hexagon: ARGSORT via bitonic sort in HVX registers for tensors up to 1024 elements |
| b9964 | Jul 11, 10:52 | Prevented duplicate spec model downloads via improved argument handling |
| b9963 | Jul 11, 08:38 | **DeepSeek-OCR v1 multi-tile support** — dynamic resolution, unified image preprocessors for both v1 and v2 |
| b9960 | Jul 11, 08:01 | Removed `loading.html` from server; UI cleanup |
| b9959 | Jul 11, 06:45 | Sync with ggml repository updates |
| b9957 | Jul 10, 20:17 | Server: removed `apply_diff`, added `tools_io` abstraction layer |

**TL;DR:** 6 builds in 22h; b9963 is the headline — DeepSeek-OCR v1 now runs multi-tile with dynamic resolution in llama.cpp, enabling document-layout-aware OCR on locally hosted GGUF models.

**Developer signal:** Three changes to action: (1) **DeepSeek-OCR v1 multi-tile (b9963)**: DeepSeek-OCR previously ran in llama.cpp in single-tile mode. The b9963 build adds multi-tile support with dynamic resolution — meaning the model now pre-processes input images into variable-size tiles (matching the original training approach) and passes them through a unified preprocessor that handles both DeepSeek-OCR v1 and v2 architectures. In practice: multi-column documents, dense tables, and multi-page scans now produce significantly better extraction accuracy. If you're running local OCR pipelines with DeepSeek-OCR GGUF models, upgrade to b9963 or later. GGUF weights compatible with PR #17400 are required. (2) **Hexagon ARGSORT HVX (b9965)**: For Qualcomm Hexagon DSP deployments, ARGSORT on small tensors (≤1024 elements) now uses an HVX-register bitonic sort instead of a general sort — meaningful throughput improvement for sampling operations on embedded/mobile Snapdragon targets. (3) **Tensor-split static regex (b9966)**: In multi-GPU setups using `-sm tensor` (tensor-split scheduling mode), the regex for tensor routing was being recompiled on every decode call. Making it static eliminates that overhead — relevant for anyone running multi-GPU inference with tensor parallelism. (4) **`apply_diff` removed (b9957)**: If you call the llama.cpp server's `apply_diff` tool endpoint directly in any automation or pipeline, this endpoint is gone as of b9957. Replace with the `tools_io` abstraction.

**Affects you if:** You run DeepSeek-OCR GGUF models locally for document OCR pipelines; you run llama.cpp on Qualcomm Hexagon DSP hardware; you use llama.cpp in multi-GPU `-sm tensor` mode; you call the llama.cpp server's `apply_diff` endpoint directly.

**Adoption effort:** Quick (binary upgrade) for most changes; Moderate if you were relying on `apply_diff` server endpoint (migration to `tools_io` required).

**Primary source:** https://github.com/ggml-org/llama.cpp/releases

*Quality gate score: 8 (official repo +3, code-level changes with specific technical impact +2, primary source link +2, in-window +1)*

---

## Benchmarks & Leaderboards

### [MEDIUM] SWE-bench Pro: GPT-5.6 Sol (64.6%), Terra (63.4%), Luna (62.7%) enter the leaderboard — all beat Muse Spark 1.1 on coding

**Source:** Scale AI / SWE-bench Pro | **Date:** Updated July 10, 2026 | **Link:** https://labs.scale.com/leaderboard/swe_bench_pro_public

**What changed:** GPT-5.6 Sol, Terra, and Luna entered the SWE-bench Pro leaderboard as of July 10, 2026 — the first published SWE-bench Pro scores for the GPT-5.6 family (launched July 9). Prior to this update, the July 10 digest had no confirmed independent SWE-bench Pro data for any GPT-5.6 model.

**TL;DR:** SWE-bench Pro standings after July 10 update: Fable 5 (80.4%) → Opus 4.8 (69.2%) → GPT-5.6 Sol (64.6%) → GPT-5.6 Terra (63.4%) → GPT-5.6 Luna (62.7%) → Muse Spark 1.1 (61.5%) → Tencent Hy3 (57.9%).

**Developer signal:** This is the first concrete dataset letting you place GPT-5.6's coding capability relative to Muse Spark 1.1 and Claude Opus 4.8 on the same benchmark. The result is unambiguous on SWE-bench Pro: all three GPT-5.6 tiers beat Muse Spark 1.1 (61.5%), with Sol clearing it by 3.1 points and even Luna (the cheapest tier at $1/$6 per MTok) clearing it by 1.2 points. At the same time, all three GPT-5.6 tiers fall 4.6–9.8 points below Claude Opus 4.8 (69.2%). For developers deciding between Muse Spark 1.1 ($1.25/$4.25 per MTok) and GPT-5.6 Luna ($1/$6) for coding tasks: Luna is now benchmarked as the stronger coding model at a comparable price point. The prior digest's Horizon entry about Muse Spark being optimized for orchestration rather than coding is now supported by a second benchmark source. Important caveats: (1) SWE-bench Pro scores for Meta models are typically self-submitted and have not been independently audited on a separate hold-out set; (2) SWE-bench Pro tests a specific type of real-world GitHub issue fixing — performance varies by task type; (3) Muse Spark 1.1 leads GPT-5.6 Sol on MCP Atlas agentic evals (88.1 vs. not yet published for Sol), so the comparison flips depending on the task.

**Affects you if:** You are evaluating GPT-5.6 vs. Muse Spark 1.1 for coding or agent-coding workloads; you track the SWE-bench Pro leaderboard for model selection decisions; you want to understand the coding-vs-orchestration capability split across major frontier APIs.

**Adoption effort:** N/A — benchmark data, no code changes required.

**Primary source:** https://labs.scale.com/leaderboard/swe_bench_pro_public

*Quality gate score: 7 (official Scale AI leaderboard +3, concrete benchmark numbers with model comparisons +2, primary source link +2; -1 for Meta self-reported SWE-bench Pro baseline caveat)*

---

## Trends & Emerging Tech

### Claude Code cloud deployment auto mode — the opt-in era ends

**Source:** Anthropic / Claude Code Changelog | **Date:** July 11, 2026 | **Link:** https://code.claude.com/docs/en/changelog

**What's happening:** Claude Code auto mode — which dynamically selects the most appropriate Claude model for each task within a session — was previously opt-in via an environment variable on all three major cloud platforms (Bedrock, Vertex AI, Foundry). With v2.1.207, it's on by default. The change mirrors what happened on the Claude Platform itself (where auto mode has been the default for consumer users for months). The cloud provider default flip completes the transition: auto mode is now the default everywhere Claude Code runs.

**Why watch this:** Teams running Claude Code in automated pipelines or CI on cloud providers need to evaluate cost and output-behavior implications before the update rolls out to their environments. Auto mode's model-switching is designed to reduce cost on routine subtasks while preserving quality on complex steps — but it changes per-session token routing in ways that can affect determinism, pricing predictability, and debugging reproducibility. The deprecation of Opus 4.7 fast mode (still tracking for July 24) compounds this: as Claude 4.7 access paths narrow, auto mode becomes the primary mechanism for routing between available Opus versions on cloud platforms.

---

### DeepSeek-OCR lands in llama.cpp with multi-tile — local document OCR gains a serious open-source stack

**Source:** ggml-org/llama.cpp GitHub Releases | **Date:** July 11, 2026 | **Link:** https://github.com/ggml-org/llama.cpp/releases/tag/b9963

**What's happening:** DeepSeek-OCR v1 multi-tile support (b9963) is the most practically significant step yet in making document OCR a first-class local inference task in llama.cpp. Previous single-tile mode worked for simple documents; multi-tile with dynamic resolution now handles multi-column layouts, dense tables, and structured PDFs in a way that's competitive with cloud OCR APIs. The model runs entirely on CPU or GPU via GGUF quantization — meaning developers can now build document extraction pipelines that never send data offsite.

**Why watch this:** The combination of local document OCR (DeepSeek-OCR GGUF + llama.cpp), local embedding (nomic-embed), and local reasoning (any recent GGUF) creates a fully local, privacy-preserving RAG pipeline that requires no cloud API calls. For developers in regulated industries (healthcare, legal, finance) where document data cannot leave the premises, this stack is now viable for production experimentation. The quality benchmark numbers for multi-tile mode on real-world layouts are not yet third-party validated in llama.cpp — that will be the signal to watch in the next few weeks.

---

## Technical Discussions

Nothing cleared the quality bar this period. No community threads on Hacker News (with score >200 and concrete technical data) or practitioner blogs from tier 2 sources were published specifically on July 10–11, 2026 within the scan window at the required quality threshold.

---

## Quick Hits

- **gemini-embedding-001 shutdown: 3 days remaining** — action deadline is July 14; migrate to `models/gemini-embedding-2` now if you haven't; see July 10 digest Breaking Changes section for full migration guide.
- **Anthropic Platform July 10 — CMEK documentation update** — the [Access Transparency](/docs/en/manage-claude/access-transparency) docs for CMEK content preservation events now include a filter example, an example event payload, and two new preservation reason codes (`policy_violation_investigation`, `csae_report`). Documentation-only update; no functional API change. https://platform.claude.com/docs/en/release-notes/overview

---

## Worth Watching (Announced, Not Yet Shipped)

| Item | ETA | Notes |
|------|-----|-------|
| **gemini-embedding-001 shutdown** | **July 14, 2026** | 3 days remaining; migrate to `gemini-embedding-2` immediately |
| **Gemini 3.5 Pro** | **~July 17, 2026 (target, unconfirmed)** | Full architectural rebuild; 2M token context, Deep Think Reasoning Layer; no official confirmation as of July 11 — treat as unconfirmed until Google publishes a model card or changelog entry |
| **Claude Opus 4.7 fast mode removal** | **July 24, 2026** | Requests to `claude-opus-4-7` with `speed: "fast"` return error after this date; migrate to Opus 4.8 fast mode; overlaps with v2.1.207 auto mode default change on cloud platforms |
| **GPT-Live-1 and GPT-Live-1 mini API** | "Coming soon" | OpenAI Developer sign-up form still open; no pricing, endpoint shape, or timeline announced |
| **Claude Opus 4.1 retirement** | August 5, 2026 | Migrate to Opus 4.8 |
| **Imagen 4 shutdown** | August 17, 2026 | Migrate to Imagen 4 Ultra or Veo 3 |

---

<details>
<summary>🔭 Horizon — Open Questions, Emerging Patterns & Grounded Speculation</summary>

> This section operates under different rules than the digest above.
> Evidence-grounded speculation is allowed. Pure prediction is not.
> Every claim here must cite a source from this digest or a real paper/benchmark.
> Label each entry by type so the reader knows what kind of thinking they're engaging with.

**[PATTERN] The SWE-bench Pro tier stack now maps cleanly to frontier API pricing tiers — coding capability and cost are converging**
With GPT-5.6 Sol (64.6%), Terra (63.4%), Luna (62.7%), and Muse Spark 1.1 (61.5%) all sitting within a 3.1-point band on SWE-bench Pro at price points ranging from $1 to $5 per MTok input, the previously fuzzy "capability vs. cost" tradeoff is now precise enough to optimize against. Claude Opus 4.8 (69.2%) remains 4.6–8.1 points ahead of the GPT-5.6/Muse Spark cluster on this benchmark at significantly higher cost, making it the benchmark-justified choice for high-stakes coding tasks. But the GPT-5.6 Luna at $1/$6 being within 1.2 points of Muse Spark 1.1 at $1.25/$4.25 means the cheaper model per MTok also has a slight coding edge — LiteLLM routing strategies that once required A/B testing can now start from a cleaner data table.
*Grounded in: GPT-5.6 Sol 64.6%, Terra 63.4%, Luna 62.7% (this digest, Benchmarks); Muse Spark 1.1 61.5% (July 10 digest, Model Releases); Opus 4.8 69.2% (July 10 digest, Benchmarks); pricing from July 9-10 digests*

---

**[TENSION] Claude Code auto mode defaults ON for cloud platforms while Opus 4.7 fast mode prepares for removal — two simultaneous model routing changes in the same deployment window**
v2.1.207 defaults auto mode ON for Bedrock/Vertex/Foundry users (July 11). On July 24, Opus 4.7 fast mode is removed — calls to `claude-opus-4-7` with `speed: "fast"` will return errors. These two changes interact: auto mode in Claude Code dynamically routes to the "best" available model for each task, which now explicitly excludes Opus 4.7 fast as a routing target after July 24. Teams that built automated pipelines or CI on Bedrock/Vertex with explicit Opus 4.7 fast calls need to address both changes in the same two-week window: migrate the explicit calls before July 24 and understand the auto mode default before that migration is complete. The combination represents the densest model-routing transition window Anthropic has shipped so far in 2026.
*Grounded in: Claude Code v2.1.207 auto mode default change (this digest, Tooling); Opus 4.7 fast mode removal July 24 (July 10 digest, Worth Watching; platform.claude.com release notes July 8)*

---

**[OPEN QUESTION] GPT-5.6 Sol's SWE-bench Pro score (64.6%) is confirmed — but where does it rank on MCP Atlas?**
Muse Spark 1.1 leads MCP Atlas at 88.1 vs. Opus 4.8's 82.2 (July 10 digest). GPT-5.6 Sol has no published MCP Atlas score — Meta's benchmark table used GPT-5.5 as the comparator and Sol was released the same day as Muse Spark 1.1. The open question: does GPT-5.6 Sol's agentic tool-orchestration capability match or exceed Muse Spark 1.1's lead on MCP Atlas, or is the coding benchmark ranking (Sol above Muse Spark) the more reliable signal of Sol's general capability? If independent MCP Atlas evaluations show Sol closer to or above Muse Spark 1.1's 88.1, the July 10 Horizon entry about Muse Spark being uniquely optimized for orchestration may need revision. Independent MCP Atlas evaluations for GPT-5.6 are the highest-value benchmark data point currently missing.
*Grounded in: Muse Spark 1.1 MCP Atlas 88.1 vs. Opus 4.8 82.2 (July 10 digest, Benchmarks); GPT-5.6 Sol SWE-bench Pro 64.6% (this digest, Benchmarks); Meta benchmark table's exclusion of Sol (July 10 digest, Horizon)*

---

**[IF THIS CONTINUES] Local document OCR is 12–18 months from replacing cloud OCR APIs for regulated workloads**
The b9963 DeepSeek-OCR v1 multi-tile addition to llama.cpp, combined with the ExecuTorch backend landing in b9951 (July 10 digest), suggests a coherent trend: the llama.cpp GGUF ecosystem is actively expanding into specialized vision tasks beyond basic CLIP-adapter image captioning. DeepSeek-OCR GGUF with multi-tile support is now the most capable open-weight, locally runnable OCR stack available. If multi-tile quality validates in the next 4–6 weeks of community benchmarking, and if further improvements (DeepSeek-OCR v2 multi-tile, layout detection) land at the same build cadence, a production-viable local pipeline for financial documents, medical records, or legal contracts — with no data leaving the deployment environment — becomes feasible within a year. The current gap vs. cloud OCR APIs (Google Document AI, Azure Document Intelligence) is primarily quality on highly structured tables and handwritten text, not architecture.
*Grounded in: llama.cpp b9963 DeepSeek-OCR v1 multi-tile (this digest, Tooling); ExecuTorch ET backend initial b9951 (July 10 digest, Tooling); Hexagon backend active development b9946/b9965 (this digest + July 10 digest)*

---

**[BUILDER'S ANGLE] Claude Code auto mode + Bedrock/Vertex as a cost-routing layer: the case for a multi-provider auto mode**
Claude Code v2.1.207 makes auto mode the default on Bedrock, Vertex, and Foundry — but auto mode currently routes only within Anthropic's model family. The pattern emerging across multiple recent releases (LiteLLM MCP OAuth v2, Meta Model API OpenAI-compatible format, GPT-5.6's three-tier pricing) creates the ingredients for a cross-provider auto mode: a routing layer that sends a task to the cheapest capable model across Anthropic, OpenAI, Meta, and xAI based on task type, context length, and latency requirement. LiteLLM already proxies all four APIs; the missing piece is per-task capability inference (e.g., "this is a coding subtask — route to Sol-tier; this is an orchestration task — route to Muse Spark 1.1"). Claude Code auto mode is the template: it demonstrates that the routing heuristic is tractable. A developer or company that builds a production-quality open-source cross-provider auto-router would solve the single most expensive decision in building agentic pipelines today.
*Grounded in: Claude Code v2.1.207 auto mode default (this digest, Tooling); GPT-5.6 Luna $1/$6 per MTok (July 9 digest); Muse Spark 1.1 $1.25/$4.25 per MTok (July 10 digest); LiteLLM MCP OAuth v2 support (July 4 digest)*

</details>

---

*Excluded: 39 items below quality gate threshold.*

*Near-misses: Anthropic Platform July 10 CMEK documentation update — docs-only, no functional API change, excluded from main sections; Claude Console API key expiration feature (July 8) — in the July 9 digest's window, not in today's 24h window; LiteLLM v1.93.0-dev.3 (July 10) — dev pre-release, unstable, excluded per policy; GPT-5.6 Sol/Terra/Luna API launch (July 9) — covered in July 9 digest; Muse Spark 1.1 + Meta Model API (July 9) — covered in July 10 digest; Grok 4.5 (July 8) — covered in July 8 digest; Gemini 3.5 Pro — not released as of July 11, target July 17; Devstral 2507 (mistral.ai/news/devstral-2507/) — confirmed July 2025 release, outside window; Mistral Vibe 2.0 (mistralai/news/mistral-vibe-2-0/) — January 27, 2026, outside window; NVIDIA Dynamo Snapshot — published June 5, 2026, outside window; NVIDIA Vera CPU blog post (July 7) — outside window; Ollama v0.31.2 (July 6) — outside window; HuggingFace hf CLI (July 25, 2025 blog post, July 9 v1.23.0 release) — blog post outside window, release July 9 in prior digest window; Cohere North Mini Code (June 9) — outside window; LateOn-Code & ColGrep (February 19, 2026) — outside window; HuggingFace Nemotron 3 Nano Omni (April 29, 2026) — outside window; Together AI ICML 2026 post (early July) — conference recap, no developer-actionable technical changes; Azure API Management MCP servers — Build 2026 era (~May 2026), outside window; arXiv cs.AI July 10–11 submissions — no papers combined recognized-lab authorship + code repo + measurable benchmark numbers within window; Show HN: GLM 5.2 on slow hardware (July 10 HN front page) — personal "how I" post, no primary source benchmark or code release; AgentPrizm launch (July 9) — outside window; Arena.ai GPT-5.6 ELO — insufficient vote accumulation within window; SWE-bench Live — no July 10-11 new entries confirmed; Nathan Lambert Interconnects.ai — no new post July 10-11 within window; Eugene Yan, Sebastian Raschka — no new posts within window*
